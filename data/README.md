# Data Folder

- `generated/`: generated benchmark-ready datasets.
- `raw/`: raw source datasets used for preprocessing.

The extension dataset generator writes outputs to `data/generated/extension_datasets/`.
